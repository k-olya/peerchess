import { SystemGroup } from "systems/system-group";

export class LobbyScene extends SystemGroup {
  constructor() {
    super("lobby", []);
  }
  onCreate() {
    super.onCreate();
  }
  onFree() {
    super.onFree();
  }
}
